# Discord Bot Configuration

# Your Discord bot token from https://discord.com/developers/applications
DISCORD_TOKEN=your_bot_token_here

# Your Discord application ID (found in the Discord Developer Portal)
DISCORD_APP_ID=your_app_id_here

# Optional: Specific guild ID for faster command registration during development
# If not set, commands will be registered globally (takes up to 1 hour to propagate)
# If set, commands register instantly but only work in that specific guild
DISCORD_GUILD_ID=

# Database Configuration
# Path to the SQLite database file (use ":memory:" for in-memory database)
DB_PATH=./data/bot.sqlite

# Application Environment
NODE_ENV=development

# Logging Level (debug, info, warn, error)
LOG_LEVEL=info
